import SwiftUI

// Xcode's "Watch App" template auto-generates this file with a struct name
// derived from your product name (e.g. `Stride_Watch_AppApp`) when you create
// the target — keep whatever it generated, just make sure its body renders
// ContentView(), same as here.
@main
struct StrideWatchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
